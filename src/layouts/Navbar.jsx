import { NavLink } from "react-router-dom";

const Navbar = () => {
    return (
        <div>
            <h1>Emart</h1>
            <div>
                <NavLink></NavLink>
            </div>
        </div>
    );
};

export default Navbar;